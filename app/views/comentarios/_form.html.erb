<%= form_for([@foto,@comentario]) do |f| %>
  <% if @comentario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comentario.errors.count, "error") %> prohibited this comentario from being saved:</h2>
      <ul>
      <% @comentario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul> 
    </div>
  <% end %>

  <div class="field">
    <p class="text-center"><%= f.label :TEXTO %></p>
    <p class="text-center"><%= f.text_field :TEXTO, placeholder:"¿Qué te pareció la foto?", class:"form-control"%></p>
  </div>
  <div class="actions">
    <p class="text-center"><%= f.submit "Comenta!", class:"btn btn-default" %></p>
  </div>
<% end %>
