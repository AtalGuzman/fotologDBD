<!DOCTYPE html>
<div class="field orange">
		<%if usuario_signed_in?%>
            <h1><em><p class="text-center"><%=current_usuario.NOMBRE_PHOTOLOG %></p><hr><br></em></h1>
			<%if current_usuario.fotos.last!=nil%>
				<div class="field">
                  <h1><em><small><p class="text-center">La última foto que subiste fue:</p></small></em></h1>
				</div>
				<div class = "field center-md">
					<h2><%= link_to current_usuario.fotos.last.TITULO,"fotos/#{current_usuario.fotos.last.id}"%></h2>
					
					<%= image_tag  current_usuario.fotos.last.imagen.url(:medium)%>
				</div>
			<%else%>
				<div class="field">
                  <h1><em><small><p class="text-center">Empieza por subir una foto: <br>
					<a color: black><%= link_to "haz click aquí!","fotos/new"%>	</a></p></small> </em></h1><!-- Entonces foto/new corresponde al view del new de la foto, entonces hay que definirle un método en el controlador y luego crearle una vista-->
				</div>
			<%end%>
				
		<%else%>

          <div id="main-container" class="container">
              <div class="row">
                <div class="col-md-4">
                  <h1><em><p class="text-center">¡Bienvenido!</p><hr>
                    <small>Infolog te permitirá comunicarte y compartir con las personas que forman parte de tu vida.</small></em></h1>
                </div>

                <div class="col-md-8">
                  <div id="myCarousel" class="carousel slide" data-ride="carousel" style="width: 600px; margin: 0 auto">
                    <!-- icators -->
                    <ol class="carousel-indicators">
                      <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
                      <li data-target="#myCarousel" data-slide-to="1"></li>
                      <li data-target="#myCarousel" data-slide-to="2"></li>
                    </ol>
                    <div class="carousel-inner" role="listbox">
                      <div class="item active">
                        <img src="<%= asset_path( 'gato2.jpg') %>" algn="middle"  class="center-block"  alt="First slide" >
                        <div class="container">
                          <div class="carousel-caption">

                          </div>
                        </div>
                      </div>
                      <div class="item">
                        <img src="<%= asset_path( 'jove.jpg') %>"  class="center-block" alt="Second slide">
                        <div class="container">
                          <div class="carousel-caption">

                          </div>
                        </div>
                      </div>
                      <div class="item">
                        <img src="<%= asset_path( 'playa.jpg') %>"  class="center-block"  alt="Third slide">
                        <div class="container">
                          <div class="carousel-caption">

                          </div>
                        </div>
                      </div>
                    </div>
                    <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
                      <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
                      <span class="sr-only">Previous</span>
                    </a>
                    <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
                      <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
                      <span class="sr-only">Next</span>
                    </a>
                  </div><!-- /.carousel -->
                </div>
              </div>
              <!-- Carousel                  ================================================== -->
          </div>
        <%end%>
</div>

